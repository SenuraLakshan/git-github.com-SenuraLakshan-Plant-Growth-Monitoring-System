import 'package:flutter/cupertino.dart';

class GrowthMonitorPage extends StatelessWidget {
  const GrowthMonitorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Growth Monitoring'));
  }
}